# 农民工管理系统 — 详细项目计划书

> 编制日期：2026-02-14

---

## 一、项目概述

### 1.1 项目目标
建设一套面向**建筑行业的农民工管理系统**，支持多项目/工地管理，涵盖人员基本信息、每日工天记录、月度薪资核算、数据报表与导出，并提供微信小程序端实现移动考勤打卡。

### 1.2 技术栈选型

| 层级 | 技术 | 说明 |
|------|------|------|
| **前端（统一）** | uni-app 3.x (Vue 3) | **一套代码**，编译到 H5 管理后台 + 微信小程序 |
| **后端 API** | Spring Boot 3.x (Java 21) | RESTful API + Swagger/SpringDoc API文档 |
| **数据库** | MySQL 8.0 | 结构化数据存储 |
| **缓存** | Redis (可选，后期引入) | Session、热点数据缓存 |
| **对象存储** | 阿里云 OSS | 人员照片、合同文件存储 |
| **部署** | 阿里云 ECS + Docker | 容器化部署，便于维护升级 |

> **前端统一策略**：使用 uni-app 同时编译 **H5 版本**（管理后台，PC/平板浏览器访问）和 **微信小程序**（工人端打卡），只需维护一个前端项目。通过条件编译 `#ifdef H5` / `#ifdef MP-WEIXIN` 处理平台差异。

### 1.3 使用规模
- 1-3 个项目工地
- < 100 名工人
- 5-10 个管理员用户
- 月预算 < 200 元（阿里云）

---

## 二、功能模块详细设计

### 2.1 系统角色

| 角色 | 权限 | 使用端 |
|------|------|--------|
| **超级管理员** | 全部权限 + 系统设置 | H5 后台 |
| **项目经理** | 本项目的所有管理权限 | H5 后台 |
| **工长/班组长** | 考勤录入、工天确认 | H5 后台 + 小程序 |
| **工人** | 打卡、查看个人工资条 | 微信小程序 |

### 2.2 模块一：基本信息管理

#### 功能清单
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 工人信息登记 | 姓名、身份证号、手机号、照片、籍贯、工种、紧急联系人 | P0 |
| 日薪设置 | 支持按工种设置默认日薪，也可按个人单独设置 | P0 |
| 合同管理 | 上传劳动合同扫描件，记录合同期限 | P1 |
| 项目/工地管理 | 项目名称、地点、开工日期、预计完工、项目状态 | P0 |
| 班组管理 | 班组名称、工长、所属项目、班组成员管理 | P0 |
| 工种字典 | 木工、钢筋工、泥瓦工、电工等工种维护 | P0 |
| 工人调动 | 工人在项目/班组间调动，保留历史记录 | P1 |

#### 适老化 UI 设计原则（工天录入等高频操作）

本系统的管理端使用者多为**中老年工长/班组长**，UI 设计需遵循以下原则：

| 原则 | 具体措施 |
|------|----------|
| **大字体、大按钮** | 全局基础字号 ≥ 16px，按钮最小高度 48px，重要操作按钮 ≥ 56px |
| **高对比度** | 前景/背景对比度 ≥ 4.5:1，使用深色文字 + 浅色背景 |
| **颜色辅助** | 不依赖颜色传达信息，同时使用图标+文字标识状态 |
| **操作极简** | 工天录入：一屏展示班组全部工人，每人只需**点击一次**选择出勤类型 |
| **减少输入** | 尽量用选择代替输入，日薪自动带入，日期默认当天 |
| **大触控区域** | 列表行高 ≥ 52px，间距充足，防止误触 |
| **即时反馈** | 操作后明确的成功/失败提示，使用 Toast + 震动 |
| **容错设计** | 批量录入支持撤销，删除需二次确认，可随时修改当天考勤 |

**工天批量录入交互设计**（核心高频页面）：
```
┌──────────────────────────────────────────────┐
│  📅 2026年2月14日(六)    [< 前一天] [后一天 >]  │
│  🏗️ 项目: [XX工地 ▼]   👥 班组: [钢筋班 ▼]    │
├──────────────────────────────────────────────┤
│                                              │
│  ┌─────────┬────────────────────────────┐    │
│  │  张三    │  [全天✓]  [半天]  [加班]  [休] │    │
│  │  ¥350/天 │                            │    │
│  ├─────────┼────────────────────────────┤    │
│  │  李四    │  [全天]  [半天✓]  [加班]  [休] │    │
│  │  ¥300/天 │                            │    │
│  ├─────────┼────────────────────────────┤    │
│  │  王五    │  [全天]  [半天]  [加班]  [休✓] │    │
│  │  ¥280/天 │                            │    │
│  └─────────┴────────────────────────────┘    │
│                                              │
│  📊 今日统计: 全天2人 半天1人 休1人             │
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │        ✅ 提 交 考 勤 (大按钮)         │    │
│  └──────────────────────────────────────┘    │
└──────────────────────────────────────────────┘

特点：
• 每人一行，点击按钮即完成录入，无需打字
• 默认"全天"预选，只需修改例外情况
• 日薪自动展示，一目了然
• 底部统计实时更新
• 提交按钮大且醒目
```

#### 数据模型（核心字段）
```
Worker (工人表)
├── id, worker_no (用工编号)
├── name, id_card, phone, photo_url
├── gender, birth_date, native_place
├── work_type_id (工种), pay_type (计薪方式: 日薪/月薪)
├── daily_wage (日薪), monthly_wage (月薪)
├── emergency_contact, emergency_phone
├── bank_account (银行卡号)
├── bank_name (开户行——具体到分行)
├── payment_method (默认发放方式: 总包代发/班组代发/现金)
├── status (在岗/离场/待岗)
└── created_at, updated_at

Project (项目/工地表)
├── id, name, location, description
├── district (区域，如"江阳区")
├── general_contractor (总承包企业)
├── subcontractor (分包企业)
├── project_manager_name (项目负责人)
├── start_date, expected_end_date
├── status (进行中/已完工/暂停)
└── created_at, updated_at

Team (班组表)
├── id, name, project_id, leader_id
└── created_at, updated_at

WorkType (工种表)
├── id, name, default_daily_wage
└── description
```

### 2.3 模块二：工天管理

#### 功能清单
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 每日考勤录入 | 管理端：按班组/项目批量录入当日出勤 | P0 |
| 手机打卡 | 小程序端：GPS定位 + 拍照打卡 | P0 |
| 工天类型 | 全天(1工)、半天(0.5工)、加班(1.5工/2工)、请假(0工) | P0 |
| 考勤审核 | 工长确认 → 项目经理审批 | P1 |
| 考勤日历 | 日历视图展示每月出勤情况 | P1 |
| 补卡申请 | 漏打卡后申请补录 | P2 |

#### 数据模型
```
Attendance (考勤/工天记录表)
├── id, worker_id, project_id, team_id
├── work_date (工作日期)
├── work_type (全天/半天/加班/请假)
├── work_days (折算工天数: 0/0.5/1/1.5/2)
├── daily_wage_snapshot (当日日薪快照)
├── is_overtime (是否加班)
├── overtime_hours (加班时长)
├── actual_work_output (当日实际工作量描述)
├── check_in_time, check_in_location, check_in_photo
├── check_out_time, check_out_location, check_out_photo
├── status (待确认/已确认/已驳回)
├── confirmed_by, confirmed_at
├── remark
└── created_at, updated_at
```

### 2.4 模块三：月度薪资管理

#### 功能清单
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 月度薪资自动核算 | 根据当月工天 × 日薪自动汇总 | P0 |
| 薪资调整 | 支持加扣项（奖金、借支、罚款等） | P0 |
| 薪资审批流程 | 工长提交 → 项目经理审核 → 确认发放 | P1 |
| 工资条查看 | 工人通过小程序查看每月工资明细 | P0 |
| 发放记录 | 记录实际发放金额、发放日期、发放方式 | P0 |
| 年度汇总 | 工人年度收入总览 | P2 |

#### 数据模型
```
MonthlySalary (月度薪资表)
├── id, worker_id, project_id
├── year, month
├── total_work_days (当月总工天)
├── daily_wage (当月适用日薪)
├── monthly_wage (当月适用月薪，月薪制时使用)
├── base_salary (基本薪资 = 工天×日薪 或 月薪)
├── overtime_pay (加班工资)
├── gross_salary (应发金额 = 基本薪资 + 加班工资)
├── previous_unpaid (上月未发金额)
├── bonus (奖金)
├── withholding (代缴金额，如保险等)
├── deduction (其他扣款)
├── advance_payment (预支/借支)
├── actual_salary (实发金额)
├── payment_method (发放方式: 总包代发/班组代发/现金)
├── worker_confirmed (工人签字确认: 是/否)
├── worker_confirmed_at (工人确认时间)
├── status (待核算/待审批/已审批/已发放)
├── generated_at (制表时间)
├── 审批流程字段（对应工资支付表多级签名）：
│   ├── team_leader_signed (班组长签名)
│   ├── subcontractor_signed (分包单位负责人签名)
│   ├── labor_officer_signed (劳资专管员签名)
│   ├── finance_signed (项目财务签名)
│   ├── business_mgr_signed (项目商务经理签名)
│   └── project_mgr_signed (项目经理签名)
├── paid_at, remark
└── created_at, updated_at

SalaryAdjustment (薪资调整明细表)
├── id, monthly_salary_id
├── type (奖金/扣款/借支/代缴/其他)
├── amount, reason
└── created_at
```

### 2.5 模块四：报表与数据导出

#### 功能清单
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 项目工天统计 | 按项目统计各工种工天数、人员出勤率 | P0 |
| 月度薪资汇总表 | 项目维度的月薪资总表 | P0 |
| 个人工天明细 | 按工人查看每月逐日出勤 | P0 |
| 人工成本分析 | 按项目/工种分析用工成本趋势 | P1 |
| 导出 Excel | 所有报表支持导出 .xlsx 格式 | P0 |
| 导出 PDF | 工资条、汇总表支持 PDF 下载 | P1 |
| 数据看板 | 首页可视化大屏：在岗人数、当月支出、出勤率等 | P2 |

---

## 三、系统架构图

```
┌────────────────────────────────────────────────────────────────┐
│                        客户端层                                 │
│                  uni-app 3.x (Vue 3) 统一前端                   │
│  ┌──────────────────┐         ┌──────────────────────────┐     │
│  │   H5 版本        │         │    微信小程序版本          │     │
│  │   管理后台        │         │   工人考勤打卡             │     │
│  │   (PC/平板浏览器) │         │   工资条查看              │     │
│  └────────┬─────────┘         └────────────┬─────────────┘     │
└───────────┼────────────────────────────────┼───────────────────┘
            │ HTTPS                          │ HTTPS
┌───────────┼────────────────────────────────┼───────────────────┐
│           ▼                                ▼                   │
│  ┌─────────────────────────────────────────────────┐           │
│  │         Nginx (反向代理 + SSL + H5静态托管)      │           │
│  └────────────────────────┬────────────────────────┘           │
│                           │                                    │
│  ┌────────────────────────▼────────────────────────┐           │
│  │      Spring Boot 3.x  (API + Swagger文档)       │           │
│  │  ┌──────────┬──────────┬──────────┬──────────┐  │           │
│  │  │ 用户认证  │ 基本信息  │ 工天管理  │ 薪资管理 │  │           │
│  │  │ JWT/微信  │  管理    │  考勤    │  报表   │  │           │
│  │  └──────────┴──────────┴──────────┴──────────┘  │           │
│  └──────┬──────────────────────────┬───────────────┘           │
│         │                          │                           │
│  ┌──────▼──────┐           ┌───────▼───────┐                  │
│  │  MySQL 8.0  │           │  阿里云 OSS    │                  │
│  │  (RDS/本地)  │           │  文件存储      │                  │
│  └─────────────┘           └───────────────┘                  │
│                                                                │
│                    阿里云 ECS (Docker)                          │
└────────────────────────────────────────────────────────────────┘
```

---

## 四、阿里云资源规划（经济型 < 200元/月）

### 4.1 资源清单

| 资源 | 规格 | 预估月费 | 说明 |
|------|------|----------|------|
| **ECS 云服务器** | 2核4G, 40GB SSD系统盘 | ~70-90元/月 | 突发性能实例 t6/t5，部署全部服务 |
| **云数据库 RDS MySQL** | 基础版 1核1G 20GB | ~50-60元/月 | 或改用 ECS 自建 MySQL 节省成本 |
| **OSS 对象存储** | 标准存储 | ~5-10元/月 | 照片、合同文件，按量计费 |
| **域名** | .com / .cn | ~50-70元/年 | 按年付费，折合每月约5元 |
| **SSL 证书** | 免费证书 / Let's Encrypt | 0元 | 阿里云提供免费单域名SSL |
| **CDN（可选）** | 流量包 | ~10元/月 | 前端静态资源加速，初期可不用 |


### 4.2 省钱策略
1. **ECS 选择包年**：1年付比月付便宜 30-50%，新用户首购更低
2. **自建 MySQL**：小规模场景可在 ECS 上用 Docker 运行 MySQL，省掉 RDS 费用（~50元/月）
3. **学生/新用户优惠**：阿里云新用户 ECS 可低至 99元/年
4. **竞价实例**：开发测试环境使用竞价实例，成本仅正价的 10-20%

### 4.3 推荐方案（总计 ≈ 100-150元/月）

```
方案A：极简部署（推荐初期）
├── ECS 2核4G (包年优惠): ~70元/月
├── ECS 内自建 MySQL (Docker): 0元 (包含在ECS内)
├── ECS 内自建 Redis (Docker): 0元 (包含在ECS内)
├── OSS 标准存储: ~5元/月
├── 域名 + 免费SSL: ~5元/月
└── 总计: ~80-100元/月

方案B：稳定部署
├── ECS 2核4G: ~80元/月
├── RDS MySQL 基础版: ~55元/月
├── OSS: ~5元/月
├── 域名 + SSL: ~5元/月
└── 总计: ~145元/月
```

---

## 五、项目开发计划

### 5.1 整体时间线

| 阶段 | 时间 | 主要交付物 |
|------|------|-----------|
| **Phase 0: 项目初始化** | 第1周 | 脚手架搭建、开发环境、CI/CD |
| **Phase 1: 核心基础** | 第2-3周 | 用户认证、基本信息管理 |
| **Phase 2: 工天管理** | 第4-5周 | 考勤录入、适老化工天管理页面 |
| **Phase 3: 薪资管理** | 第6-7周 | 月度薪资核算、工资条 |
| **Phase 4: 报表导出** | 第8周 | 统计报表、Excel/PDF导出 |
| **Phase 5: 小程序端适配** | 第9-10周 | 条件编译、打卡、小程序审核 |
| **Phase 6: 部署上线** | 第12周 | 阿里云部署、域名SSL配置 |
| **Phase 7: 优化迭代** | 持续 | 性能优化、功能迭代 |

### 5.2 Phase 0：项目初始化（第1周）

#### 项目目录结构
```
EmployeeManagementSystem/
├── backend/                         # Spring Boot 3.x (Java 21)
│   ├── src/main/java/com/henrywang/ems/
│   │   ├── config/                  # 配置类（Security, CORS, SwaggerConfig）
│   │   ├── controller/              # 控制器（@Tag, @Operation Swagger注解）
│   │   ├── service/                 # 业务逻辑
│   │   ├── repository/              # Spring Data JPA Repository
│   │   ├── entity/                  # 实体类
│   │   ├── dto/                     # 数据传输对象
│   │   ├── vo/                      # 视图对象
│   │   ├── common/                  # 通用工具类、异常处理、响应封装
│   │   └── security/                # JWT认证、权限控制
│   ├── src/main/resources/
│   │   ├── application.yml          # Swagger/SpringDoc 配置
│   │   ├── application-dev.yml
│   │   ├── application-prod.yml
│   │   └── db/migration/            # Flyway 数据库版本管理
│   └── pom.xml
├── frontend/                        # uni-app 3.x (Vue 3) — 统一前端
│   ├── src/
│   │   ├── pages/                   # 页面（H5 + 小程序共用）
│   │   │   ├── login/               # 登录（H5账号密码 / 小程序微信授权）
│   │   │   ├── home/                # 首页/仪表盘
│   │   │   ├── worker/              # 工人管理（H5管理端）
│   │   │   ├── project/             # 项目管理（H5管理端）
│   │   │   ├── attendance/          # 考勤/工天管理（适老化大按钮）
│   │   │   ├── attendance-input/    # 工天批量录入（极简交互）
│   │   │   ├── checkin/             # GPS+拍照打卡（小程序端）
│   │   │   ├── salary/              # 薪资管理 / 工资条
│   │   │   ├── report/              # 报表（H5管理端）
│   │   │   ├── profile/             # 个人中心
│   │   │   └── settings/            # 系统设置（H5管理端）
│   │   ├── components/              # 可复用组件
│   │   │   ├── elderly-ui/          # 适老化专用组件（大按钮等）
│   │   │   └── common/              # 通用组件
│   │   ├── store/                   # Pinia 状态管理
│   │   ├── api/                     # 接口封装（对接 Swagger API）
│   │   ├── hooks/                   # Composition API Hooks
│   │   ├── utils/                   # 工具函数
│   │   ├── platform/                # 平台差异代码
│   │   │   ├── h5/                  # H5 专属逻辑
│   │   │   └── mp-weixin/           # 小程序专属逻辑
│   │   ├── static/                  # 静态资源
│   │   ├── App.vue
│   │   ├── main.js
│   │   ├── manifest.json            # uni-app 配置（H5 + MP-WEIXIN）
│   │   ├── pages.json               # 路由与页面配置
│   │   └── uni.scss                 # 全局样式（大字体、高对比度基准）
│   ├── package.json
│   └── vite.config.js
├── docker/
│   ├── docker-compose.yml
│   ├── Dockerfile.backend
│   ├── Dockerfile.frontend          # 构建 H5 静态文件
│   └── nginx.conf                   # 托管 H5 + 反向代理 API
├── docs/
│   └── 项目计划书.md
└── README.md
```

> **不再需要维护两个前端项目**，`frontend/` 目录通过 `npm run build:h5` 和 `npm run build:mp-weixin` 分别编译管理后台和小程序。

#### 核心依赖
**后端 (Maven — Java 21)**
- Spring Boot 3.x Starter (Web, Security, Validation, Data JPA)
- **SpringDoc OpenAPI 2.x** (Swagger UI，访问 `/swagger-ui.html`)
- Spring Data JPA (数据持久化)
- MySQL Connector
- JWT (jjwt 0.12+)
- Flyway (数据库迁移)
- Apache POI (Excel导出)
- iText / OpenPDF (PDF导出)
- Hutool (工具类)
- Aliyun OSS SDK

> **Swagger 配置要点**：
> - 添加 `springdoc-openapi-starter-webmvc-ui` 依赖
> - Controller 使用 `@Tag`、`@Operation`、`@Schema` 注解
> - 访问地址：`http://localhost:8080/swagger-ui.html`
> - API JSON：`http://localhost:8080/v3/api-docs`

**前端 (npm — uni-app 统一项目)**
- uni-app 3.x (Vue 3 + Vite + CLI 模式)
- uni-ui (官方组件库，H5/小程序兼容性最佳)
- uView Plus 3.x (增强UI组件库，适配 Vue 3)
- Pinia (状态管理)
- Day.js (日期处理)
- ECharts (图表，H5端报表看板)
- uni.request 拦截器封装 (统一网络请求)
- uni.getLocation() (小程序定位API)
- uni.chooseImage() (小程序相机API)

> **条件编译**：使用 `#ifdef H5` / `#ifdef MP-WEIXIN` 区分平台，例如：
> - H5 端：展示完整管理后台菜单、报表图表、数据导出
> - 小程序端：只展示打卡、考勤查看、工资条

### 5.3 Phase 1-4 开发任务分解

#### Phase 1：用户认证 + 基本信息（第2-3周）
- [ ] 用户名密码登录 (H5管理端，BCrypt加密、JWT Token签发)
- [ ] 微信授权登录 (小程序端，uni.login → code换openid → JWT)
- [ ] Token刷新机制 (Access Token + Refresh Token)
- [ ] 账号绑定：微信用户首次登录自动创建账号，关联worker_id
- [ ] RBAC 角色权限系统
- [ ] 工种字典 CRUD
- [ ] 项目/工地 CRUD
- [ ] 班组 CRUD
- [ ] 工人信息 CRUD（含照片上传 OSS）
- [ ] 工人日薪设置（支持个人/工种默认）
- [ ] 前端管理页面：工人列表、详情、新增编辑

#### Phase 2：工天管理（第4-5周）
- [ ] 每日考勤录入接口（单个/批量）
- [ ] 工天类型管理（全天/半天/加班/请假）
- [ ] 考勤审核流程
- [ ] 考勤日历视图
- [ ] 按日期/班组/项目 查询考勤
- [ ] 前端考勤管理页面（适老化大按钮、大字体）
- [ ] 工天批量录入页面（一行一人、点选即录、默认全天）

#### Phase 3：薪资管理（第6-7周）
- [ ] 月度薪资自动核算逻辑
- [ ] 薪资调整（奖金/扣款/借支）
- [ ] 薪资审批流程
- [ ] 工资条生成
- [ ] 发放记录管理
- [ ] 前端薪资管理页面

#### Phase 4：报表导出（第8周）
- [ ] 项目工天统计报表
- [ ] 月度薪资汇总报表
- [ ] 个人工天明细报表
- [ ] Excel 导出 (Apache POI)
- [ ] PDF 导出 (OpenPDF)
- [ ] 首页数据看板（图表）

### 5.4 Phase 5：微信小程序端适配（第9-10周）

> 此时 H5 管理后台已完成，前端代码已存在。本阶段重点是**条件编译适配**和小程序专属功能。

- [ ] manifest.json 配置小程序 AppID、权限
- [ ] 条件编译：`#ifdef MP-WEIXIN` 隐藏管理端菜单/报表
- [ ] 微信授权登录 (uni.login) + 手机号绑定
- [ ] GPS定位打卡（上班/下班）+ 围栏校验
- [ ] 拍照打卡 (uni.chooseImage + 上传OSS)
- [ ] 小程序端考勤日历 + 工资条查看
- [ ] 小程序端个人中心
- [ ] 隐私协议配置 + 审核提交

### 5.5 Phase 6：部署上线（第12周）
- [ ] Docker 镜像构建 (后端 + H5前端 + MySQL + Nginx)
- [ ] docker-compose 编排
- [ ] 阿里云 ECS 环境搭建
- [ ] Nginx 反向代理 + SSL 证书配置
- [ ] 域名解析配置
- [ ] OSS Bucket 创建与配置
- [ ] 数据备份方案 (定时 mysqldump → OSS)
- [ ] 小程序审核发布

---

## 六、数据库完整 ER 设计

```sql
-- ============================================
-- 农民工管理系统 数据库设计 (MySQL 8.0)
-- ============================================

-- 1. 用户表 (系统登录用户，支持用户名密码 + 微信授权两种登录方式)
CREATE TABLE sys_user (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE COMMENT '登录用户名(用户名密码登录时必填)',
    password VARCHAR(255) COMMENT '密码哈希(用户名密码登录时必填)',
    real_name VARCHAR(50),
    phone VARCHAR(20),
    avatar VARCHAR(500) COMMENT '头像 URL',
    role ENUM('SUPER_ADMIN', 'PROJECT_MANAGER', 'FOREMAN', 'WORKER') NOT NULL,
    worker_id BIGINT COMMENT '关联工人ID（工人角色时）',
    -- 微信登录相关字段
    openid VARCHAR(100) COMMENT '微信小程序 openid',
    unionid VARCHAR(100) COMMENT '微信 unionid (多端统一标识)',
    wx_nickname VARCHAR(100) COMMENT '微信昵称',
    wx_avatar VARCHAR(500) COMMENT '微信头像',
    session_key VARCHAR(200) COMMENT '微信 session_key (加密存储，解密用户数据用)',
    status TINYINT DEFAULT 1 COMMENT '1:正常 0:禁用',
    last_login_at DATETIME COMMENT '最后登录时间',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_openid (openid),
    INDEX idx_phone (phone)
);

-- 2. 工种表
CREATE TABLE work_type (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) NOT NULL UNIQUE COMMENT '工种名称',
    default_daily_wage DECIMAL(10,2) COMMENT '默认日薪',
    description VARCHAR(200),
    status TINYINT DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 3. 项目/工地表
CREATE TABLE project (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL COMMENT '项目名称',
    district VARCHAR(50) COMMENT '区域（如江阳区）',
    location VARCHAR(200) COMMENT '项目地址',
    general_contractor VARCHAR(100) COMMENT '总承包企业',
    subcontractor VARCHAR(100) COMMENT '分包企业',
    project_manager_name VARCHAR(50) COMMENT '项目负责人姓名',
    longitude DECIMAL(10,7) COMMENT '经度（打卡围栏）',
    latitude DECIMAL(10,7) COMMENT '纬度（打卡围栏）',
    fence_radius INT DEFAULT 500 COMMENT '打卡围栏半径(米)',
    description TEXT,
    start_date DATE,
    expected_end_date DATE,
    actual_end_date DATE,
    status ENUM('ACTIVE', 'COMPLETED', 'PAUSED') DEFAULT 'ACTIVE',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 4. 班组表
CREATE TABLE team (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL COMMENT '班组名称',
    project_id BIGINT NOT NULL COMMENT '所属项目',
    leader_id BIGINT COMMENT '工长/班组长',
    description VARCHAR(200),
    status TINYINT DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (project_id) REFERENCES project(id),
    FOREIGN KEY (leader_id) REFERENCES worker(id)
);

-- 5. 工人表
CREATE TABLE worker (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    worker_no VARCHAR(20) COMMENT '用工编号（项目内编号）',
    name VARCHAR(50) NOT NULL COMMENT '姓名',
    id_card VARCHAR(18) COMMENT '身份证号',
    phone VARCHAR(20) COMMENT '手机号',
    gender TINYINT COMMENT '1:男 2:女',
    birth_date DATE,
    native_place VARCHAR(100) COMMENT '籍贯',
    photo_url VARCHAR(500) COMMENT '照片URL(OSS)',
    work_type_id BIGINT COMMENT '工种',
    pay_type ENUM('DAILY', 'MONTHLY') DEFAULT 'DAILY' COMMENT '计薪方式:日薪/月薪',
    daily_wage DECIMAL(10,2) COMMENT '个人日薪(日薪制)',
    monthly_wage DECIMAL(12,2) COMMENT '个人月薪(月薪制)',
    current_project_id BIGINT COMMENT '当前项目',
    current_team_id BIGINT COMMENT '当前班组',
    emergency_contact VARCHAR(50) COMMENT '紧急联系人',
    emergency_phone VARCHAR(20) COMMENT '紧急联系电话',
    bank_account VARCHAR(30) COMMENT '银行卡号',
    bank_name VARCHAR(100) COMMENT '开户行（具体到分行，如“招商银行泸州支行”）',
    default_payment_method VARCHAR(20) DEFAULT '总包代发' COMMENT '默认发放方式',
    status ENUM('ACTIVE', 'LEFT', 'IDLE') DEFAULT 'ACTIVE' COMMENT '在岗/离场/待岗',
    join_date DATE COMMENT '入场日期',
    leave_date DATE COMMENT '离场日期',
    remark VARCHAR(500),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (work_type_id) REFERENCES work_type(id),
    FOREIGN KEY (current_project_id) REFERENCES project(id)
);

-- 6. 工人调动记录表
CREATE TABLE worker_transfer (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    worker_id BIGINT NOT NULL,
    from_project_id BIGINT,
    to_project_id BIGINT,
    from_team_id BIGINT,
    to_team_id BIGINT,
    transfer_date DATE,
    reason VARCHAR(200),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (worker_id) REFERENCES worker(id)
);

-- 7. 考勤/工天记录表
CREATE TABLE attendance (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    worker_id BIGINT NOT NULL,
    project_id BIGINT NOT NULL,
    team_id BIGINT,
    work_date DATE NOT NULL COMMENT '工作日期',
    work_category ENUM('FULL_DAY', 'HALF_DAY', 'OVERTIME', 'LEAVE') NOT NULL COMMENT '出勤类型',
    work_days DECIMAL(3,1) NOT NULL COMMENT '折算工天数',
    daily_wage_snapshot DECIMAL(10,2) NOT NULL COMMENT '当日日薪快照',
    is_overtime TINYINT DEFAULT 0 COMMENT '是否加班',
    overtime_hours DECIMAL(4,1) COMMENT '加班时长(小时)',
    actual_work_output VARCHAR(200) COMMENT '当日实际工作量描述',
    check_in_time DATETIME COMMENT '上班打卡时间',
    check_in_lng DECIMAL(10,7) COMMENT '打卡经度',
    check_in_lat DECIMAL(10,7) COMMENT '打卡纬度',
    check_in_photo VARCHAR(500) COMMENT '打卡照片URL',
    check_out_time DATETIME COMMENT '下班打卡时间',
    check_out_lng DECIMAL(10,7),
    check_out_lat DECIMAL(10,7),
    check_out_photo VARCHAR(500),
    status ENUM('PENDING', 'CONFIRMED', 'REJECTED') DEFAULT 'PENDING',
    confirmed_by BIGINT,
    confirmed_at DATETIME,
    remark VARCHAR(500),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (worker_id) REFERENCES worker(id),
    FOREIGN KEY (project_id) REFERENCES project(id),
    UNIQUE KEY uk_worker_date (worker_id, work_date)
);

-- 8. 月度薪资表 (对应工资支付表)
CREATE TABLE monthly_salary (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    worker_id BIGINT NOT NULL,
    project_id BIGINT NOT NULL,
    year INT NOT NULL,
    month INT NOT NULL,
    total_work_days DECIMAL(5,1) NOT NULL COMMENT '当月总工天(考勤工作时间)',
    actual_work_output VARCHAR(200) COMMENT '当月产生实际工作量',
    daily_wage DECIMAL(10,2) COMMENT '当月适用日薪(元/天)',
    monthly_wage DECIMAL(12,2) COMMENT '当月适用月薪(元/月)',
    base_salary DECIMAL(12,2) NOT NULL COMMENT '基本薪资(工天×日薪 或 月薪)',
    overtime_pay DECIMAL(10,2) DEFAULT 0 COMMENT '加班工资',
    gross_salary DECIMAL(12,2) NOT NULL COMMENT '应发金额(基本薪资+加班工资)',
    previous_unpaid DECIMAL(10,2) DEFAULT 0 COMMENT '上月未发金额',
    bonus DECIMAL(10,2) DEFAULT 0 COMMENT '奖金',
    withholding DECIMAL(10,2) DEFAULT 0 COMMENT '代缴金额(保险等)',
    deduction DECIMAL(10,2) DEFAULT 0 COMMENT '其他扣款',
    advance_payment DECIMAL(10,2) DEFAULT 0 COMMENT '预支/借支',
    actual_salary DECIMAL(12,2) NOT NULL COMMENT '实发金额',
    payment_method VARCHAR(30) DEFAULT '总包代发' COMMENT '发放方式',
    -- 工人签字确认
    worker_confirmed TINYINT DEFAULT 0 COMMENT '工人签字确认 0:未确认 1:已确认',
    worker_confirmed_at DATETIME COMMENT '工人确认时间',
    -- 多级审批签名 (对应工资支付表底部签名栏)
    team_leader_id BIGINT COMMENT '班组长签名',
    team_leader_signed_at DATETIME,
    subcontractor_signer VARCHAR(50) COMMENT '分包单位负责人签名',
    subcontractor_signed_at DATETIME,
    labor_officer_id BIGINT COMMENT '劳资专管员签名',
    labor_officer_signed_at DATETIME,
    finance_signer_id BIGINT COMMENT '项目财务签名',
    finance_signed_at DATETIME,
    business_mgr_id BIGINT COMMENT '项目商务经理签名',
    business_mgr_signed_at DATETIME,
    project_mgr_id BIGINT COMMENT '项目经理签名',
    project_mgr_signed_at DATETIME,
    status ENUM('DRAFT', 'PENDING_APPROVAL', 'APPROVED', 'PAID') DEFAULT 'DRAFT',
    generated_at DATETIME COMMENT '制表时间',
    paid_at DATETIME,
    remark VARCHAR(500),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (worker_id) REFERENCES worker(id),
    FOREIGN KEY (project_id) REFERENCES project(id),
    UNIQUE KEY uk_worker_month (worker_id, project_id, year, month)
);

-- 9. 薪资调整明细表
CREATE TABLE salary_adjustment (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    monthly_salary_id BIGINT NOT NULL,
    type ENUM('BONUS', 'DEDUCTION', 'ADVANCE', 'WITHHOLDING', 'OTHER') NOT NULL,
    amount DECIMAL(10,2) NOT NULL,
    reason VARCHAR(200),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (monthly_salary_id) REFERENCES monthly_salary(id)
);

-- 10. 合同表
CREATE TABLE contract (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    worker_id BIGINT NOT NULL,
    project_id BIGINT,
    contract_no VARCHAR(50),
    file_url VARCHAR(500) COMMENT '合同文件URL(OSS)',
    start_date DATE,
    end_date DATE,
    status ENUM('ACTIVE', 'EXPIRED', 'TERMINATED') DEFAULT 'ACTIVE',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (worker_id) REFERENCES worker(id)
);
```

---

## 七、API 接口规划

### 7.1 RESTful API 设计

```
# ===== 认证模块 =====
POST   /api/auth/login                    # 账号密码登录
POST   /api/auth/wx-login                 # 微信小程序登录
POST   /api/auth/refresh                  # 刷新Token
GET    /api/auth/profile                  # 获取当前用户信息

# ===== 工种管理 =====
GET    /api/work-types                    # 工种列表
POST   /api/work-types                    # 新增工种
PUT    /api/work-types/{id}               # 编辑工种
DELETE /api/work-types/{id}               # 删除工种

# ===== 项目管理 =====
GET    /api/projects                      # 项目列表
POST   /api/projects                      # 新增项目
GET    /api/projects/{id}                 # 项目详情
PUT    /api/projects/{id}                 # 编辑项目
DELETE /api/projects/{id}                 # 删除项目
GET    /api/projects/{id}/workers         # 项目下工人列表
GET    /api/projects/{id}/teams           # 项目下班组列表

# ===== 班组管理 =====
GET    /api/teams                         # 班组列表
POST   /api/teams                         # 新增班组
PUT    /api/teams/{id}                    # 编辑班组
POST   /api/teams/{id}/members           # 添加班组成员
DELETE /api/teams/{id}/members/{workerId} # 移除班组成员

# ===== 工人管理 =====
GET    /api/workers                       # 工人列表（分页+搜索）
POST   /api/workers                       # 新增工人
GET    /api/workers/{id}                  # 工人详情
PUT    /api/workers/{id}                  # 编辑工人
PUT    /api/workers/{id}/wage             # 修改日薪
POST   /api/workers/{id}/transfer         # 工人调动
POST   /api/workers/upload-photo          # 上传照片

# ===== 考勤/工天管理 =====
GET    /api/attendances                   # 考勤记录列表
POST   /api/attendances                   # 录入考勤（单个）
POST   /api/attendances/batch             # 批量录入考勤
PUT    /api/attendances/{id}              # 修改考勤
POST   /api/attendances/{id}/confirm      # 确认考勤
POST   /api/attendances/{id}/reject       # 驳回考勤
POST   /api/attendances/check-in          # 小程序打卡(上班)
POST   /api/attendances/check-out         # 小程序打卡(下班)
GET    /api/attendances/calendar          # 考勤日历数据

# ===== 月度薪资 =====
GET    /api/salaries                      # 薪资列表
POST   /api/salaries/calculate            # 核算月度薪资
GET    /api/salaries/{id}                 # 薪资详情
PUT    /api/salaries/{id}                 # 修改薪资
POST   /api/salaries/{id}/adjust          # 添加调整项
POST   /api/salaries/{id}/approve         # 审批通过
POST   /api/salaries/{id}/pay             # 标记已发放
GET    /api/salaries/payslip/{id}         # 工资条详情

# ===== 报表 =====
GET    /api/reports/attendance-summary     # 工天统计
GET    /api/reports/salary-summary         # 薪资汇总
GET    /api/reports/worker-detail          # 个人明细
GET    /api/reports/cost-analysis          # 成本分析
GET    /api/reports/dashboard              # 首页看板数据
GET    /api/reports/export/excel           # 导出Excel
GET    /api/reports/export/pdf             # 导出PDF
```

---

## 八、部署方案

### 8.1 Docker Compose 编排

```yaml
# docker-compose.yml
version: '3.8'

services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - backend
      - frontend

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - DB_HOST=mysql
      - DB_PORT=3306
    depends_on:
      - mysql
    restart: always

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    command: >
      sh -c "npm run build:h5"    # 构建 H5 静态文件，Nginx 托管
    environment:
      - VITE_API_BASE_URL=http://backend:8080
    restart: always

  mysql:
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=${DB_ROOT_PASSWORD}
      - MYSQL_DATABASE=ems
      - MYSQL_USER=ems_user
      - MYSQL_PASSWORD=${DB_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    restart: always

volumes:
  mysql_data:
```

### 8.2 阿里云部署步骤

```
1. 购买 ECS 实例
   ├── 地域：就近选择（华东/华南）
   ├── 规格：ecs.t6-c1m2.large (2核4G)
   ├── 系统：Ubuntu 22.04 / CentOS 8
   └── 安全组：开放 80, 443, 22 端口

2. 环境初始化
   ├── 安装 Docker + Docker Compose
   ├── 配置防火墙
   └── 创建工作目录

3. 域名与证书
   ├── 购买/备案域名
   ├── DNS解析指向 ECS 公网IP
   └── 申请免费SSL证书 / Let's Encrypt

4. OSS 配置
   ├── 创建 Bucket（私有读写）
   ├── 配置跨域 CORS
   └── 创建 RAM 子账号（最小权限）

5. 部署应用
   ├── git clone 代码到服务器
   ├── 配置 .env 环境变量
   ├── docker-compose up -d
   └── 验证各服务运行状态

6. 数据备份
   ├── Crontab 定时 mysqldump
   ├── 备份文件上传 OSS
   └── 保留最近30天备份
```

---

## 九、安全与合规

| 项目 | 措施 |
|------|------|
| 身份证号 | AES加密存储，脱敏展示 |
| 密码 | BCrypt 加盐哈希 |
| API认证 | JWT Token + 刷新机制 |
| 接口安全 | HTTPS + CORS限制 + 请求频率限制 |
| SQL注入 | JPA/Hibernate 参数化查询 |
| XSS防护 | 输入过滤 + 输出转义 |
| 数据备份 | 每日自动备份 + OSS异地存储 |
| 操作日志 | 关键操作记录审计日志 |

---

## 十、微信小程序要点

### 10.1 为什么选择 uni-app

| 对比维度 | uni-app | Taro | 原生开发 |
|---------|---------|------|----------|
| **语法** | Vue 3（国内开发者最熟悉） | React/Vue | WXML + WXS |
| **微信兼容性** | ⭐⭐⭐⭐⭐ 最佳 | ⭐⭐⭐⭐ 良好 | ⭐⭐⭐⭐⭐ 原生 |
| **组件库生态** | uni-ui / uView 等 200+ 插件 | Taro UI（较少） | WeUI（有限） |
| **插件市场** | DCloud 插件市场 5000+ | npm 生态 | 较少 |
| **多端扩展** | 微信/支付宝/H5/App 一套代码 | 微信/H5/RN | 仅微信 |
| **IDE支持** | HBuilderX 可视化调试 | VS Code | 微信开发者工具 |
| **学习成本** | 低（会Vue即可） | 中等 | 较高 |
| **社区活跃度** | 国内最大、中文文档完善 | 较活跃 | 官方文档 |

> **结论**：uni-app 在微信小程序的兼容性、生态、开发效率上综合最优。**更关键的是**，uni-app 支持同时编译 H5 和小程序，因此本项目**使用 uni-app 作为唯一的前端框架**，一套代码维护管理后台 (H5) + 工人端 (小程序)，大幅降低维护成本。

### 10.2 技术方案 — 统一前端

```
框架：uni-app 3.x (Vue 3 + Composition API)
构建：Vite (CLI 模式)
UI库：uni-ui (官方) + uView Plus 3.x
状态管理：Pinia
网络请求：uni.request 封装 (拦截器 + Token 自动注入)
路由管理：pages.json 声明式路由
CSS方案：uni.scss + rpx 适配

构建命令：
  npm run dev:h5            # 本地H5开发
  npm run dev:mp-weixin     # 本地小程序开发
  npm run build:h5          # 构建H5产物 (部署到Nginx)
  npm run build:mp-weixin   # 构建小程序产物 (导入微信开发者工具上传)
```

**平台差异处理策略**：
```javascript
// 示例：登录页根据平台展示不同登录方式
// #ifdef H5
<UsernamePasswordLogin />      // H5端：用户名 + 密码登录
// #endif
// #ifdef MP-WEIXIN
<WechatAuthLogin />            // 小程序：微信一键授权登录
// #endif

// 示例：菜单根据平台展示不同导航
// #ifdef H5
tabBar: ["首页", "工人管理", "考勤录入", "薪资", "报表", "设置"]
// #endif
// #ifdef MP-WEIXIN
tabBar: ["首页", "打卡", "考勤", "我的"]
// #endif
```

### 10.3 需要申请的资源
| 资源 | 说明 |
|------|------|
| 微信小程序账号 | 需企业资质认证（300元/年）|
| 服务器域名备案 | 小程序要求后端域名已完成ICP备案 |
| SSL证书 | 小程序要求 HTTPS |
| 位置权限 | 需要在小程序后台配置 `wx.getLocation` 接口权限 |
| 用户隐私协议 | 2023年起微信要求配置隐私保护指引 |

### 10.4 核心页面与路由配置
```json
// pages.json
{
  "pages": [
    { "path": "pages/login/index",      "style": { "navigationBarTitleText": "登录" } },
    { "path": "pages/home/index",       "style": { "navigationBarTitleText": "首页" } },
    { "path": "pages/checkin/index",    "style": { "navigationBarTitleText": "打卡" } },
    { "path": "pages/attendance/index", "style": { "navigationBarTitleText": "考勤记录" } },
    { "path": "pages/salary/index",     "style": { "navigationBarTitleText": "工资条" } },
    { "path": "pages/salary/detail",    "style": { "navigationBarTitleText": "薪资明细" } },
    { "path": "pages/profile/index",    "style": { "navigationBarTitleText": "我的" } },
    { "path": "pages/messages/index",   "style": { "navigationBarTitleText": "消息" } }
  ],
  "tabBar": {
    "list": [
      { "pagePath": "pages/home/index",       "text": "首页",   "iconPath": "...", "selectedIconPath": "..." },
      { "pagePath": "pages/checkin/index",    "text": "打卡",   "iconPath": "...", "selectedIconPath": "..." },
      { "pagePath": "pages/attendance/index", "text": "考勤",   "iconPath": "...", "selectedIconPath": "..." },
      { "pagePath": "pages/profile/index",    "text": "我的",   "iconPath": "...", "selectedIconPath": "..." }
    ]
  }
}
```

### 10.5 关键功能实现要点

**GPS打卡**
```javascript
// 获取位置 + 围栏校验
const location = await uni.getLocation({ type: 'gcj02', isHighAccuracy: true })
const distance = calcDistance(location, projectFence) // 与项目围栏中心点距离
if (distance > project.fenceRadius) {
  uni.showToast({ title: '不在打卡范围内', icon: 'error' })
  return
}
```

**拍照打卡**
```javascript
// 拍照上传
const { tempFilePaths } = await uni.chooseImage({ count: 1, sourceType: ['camera'] })
const uploadRes = await uni.uploadFile({
  url: `${BASE_URL}/api/attendances/check-in`,
  filePath: tempFilePaths[0],
  name: 'photo',
  formData: { lng: location.longitude, lat: location.latitude }
})
```

**微信登录**
```javascript
// uni-app 微信登录流程
const { code } = await uni.login({ provider: 'weixin' })
const { token, userInfo } = await request.post('/api/auth/wx-login', { code })
uni.setStorageSync('token', token)
```

---

## 十一、所需资源汇总清单

### 11.1 阿里云资源

| # | 资源名称 | 规格 | 数量 | 预估费用 |
|---|---------|------|------|---------|
| 1 | ECS 云服务器 | 2核4G 40G SSD | 1台 | ~70-90元/月 |
| 2 | OSS 对象存储 | 标准存储 | 1个Bucket | ~5元/月 |
| 3 | 域名 | .com 或 .cn | 1个 | ~55元/年 |
| 4 | SSL证书 | 免费单域名 | 1个 | 免费 |
| 5 | ICP备案 | -- | 1次 | 免费(需时间) |


### 11.2 第三方资源

| # | 资源名称 | 说明 | 费用 |
|---|---------|------|------|
| 1 | 微信小程序账号 | 企业认证 | 300元/年 |
| 2 | 微信开放平台 | 小程序登录 | 免费 |
| 3 | Git 代码仓库 | Gitee/GitHub | 免费 |

### 11.3 开发工具

| # | 工具 | 用途 |
|---|------|------|
| 1 | IntelliJ IDEA / VS Code | 后端开发 (Java 21) |
| 2 | HBuilderX / VS Code | 前端开发 (uni-app) |
| 3 | 微信开发者工具 | 小程序真机调试 |
| 4 | Navicat / DBeaver | 数据库管理 |
| 5 | Swagger UI (内置) | API 在线文档与测试 |
| 6 | Docker Desktop | 本地容器化开发 |

---

## 十二、风险与建议

| 风险 | 影响 | 应对策略 |
|------|------|---------|
| ICP备案周期长 | 域名无法使用，小程序无法上线 | 提前2-3周启动备案流程 |
| 微信小程序审核驳回 | 延误上线 | 熟悉审核规范，首次提交最小功能版本 |
| 数据丢失 | 工资数据不可恢复 | 每日自动备份 + OSS异地存储 |
| 服务器单点故障 | 系统不可用 | Docker自动重启 + 监控告警 |
| 并发打卡高峰 | 早上集中打卡卡顿 | 合理的数据库索引 + 后期引入Redis缓存 |

---

> **下一步行动**：确认本计划后，即可开始 Phase 0 的项目脚手架搭建工作。
